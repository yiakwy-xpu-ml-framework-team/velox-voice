# DGX Spark (GB10, sm_121a) / H800 (Hopper, sm_90) — CUDA lane.
# torch must come from the cu130 wheel index (CUDA 13.0 toolkit on the box):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu130

-r requirements.txt
torch
torchaudio
